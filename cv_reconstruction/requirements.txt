# requirements.txt

# ── PyTorch (MPS backend ships with the standard macOS wheel since 1.12) ─────
torch>=2.1.0
torchvision>=0.16.0

# ── HuggingFace (Depth Anything V2) ──────────────────────────────────────────
transformers>=4.40.0
timm>=0.9.0

# ── Feature matching ──────────────────────────────────────────────────────────
# LightGlue + SuperPoint:
#   pip install git+https://github.com/cvg/LightGlue.git
lightglue>=0.1.0

# ── Object detection / segmentation ──────────────────────────────────────────
ultralytics>=8.2.0          # YOLOv11 (installs as "ultralytics")

# ── Face recognition (ONNX Runtime with CoreML provider for Apple Silicon) ───
insightface>=0.7.3
onnxruntime>=1.17.0         # CPU provider
# onnxruntime-silicon        # optional: CoreML-accelerated builds

# ── 3-D reconstruction ────────────────────────────────────────────────────────
# DUSt3R must be installed from source — it is not on PyPI:
#   git clone https://github.com/naver/dust3r
#   pip install -e dust3r/
open3d>=0.18.0              # Point cloud I/O and visualisation

# ── Computer Vision & numerics ────────────────────────────────────────────────
opencv-python-headless>=4.9.0
numpy>=1.26.0
scipy>=1.12.0
Pillow>=10.3.0

# ── Liveness detection ────────────────────────────────────────────────────────
scikit-image>=0.22.0        # LBP texture features

# ── API layer ─────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9     # required for FastAPI File() uploads